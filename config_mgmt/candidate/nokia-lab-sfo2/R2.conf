# R2 Configuration - Post R1-R4 failure optimization
# Updated: 2026-04-22 06:07:29 UTC
# Reason: IGP metric adjustment after R1-R4 link failure

configure
    router isis 1
        interface "to-R3"
            level 2 
                metric 10
            exit
        exit
        interface "to-R5"
            level 2
                metric 8  # Reduced from 10 to encourage R2-R5 path
            exit
        exit
        interface "to-R1" 
            level 2
                metric 10
            exit
        exit
    exit
commit