# ORCA v2 config proposal — v2-cfg-1776999674529
# Auto-generated on approve. Branch: v2/cfg/slice-a-qos-protection-on-upf-01-20260424-030121

title: "Slice-A QoS protection on UPF-01"
reason: |
  Dual root cause on slice-A: (1) load-balancer session skew concentrates the enterprise cohort on UPF-01 with UPF-02 carrying zero slice-A sessions, (2) QER enforcement on UPF-01 priority class has drifted -36% from intent (50 → 32 Mbps GBR). Combined effect: p99 N3 latency climbing 11.2ms → trending toward 15ms SLA breach in ~4 minutes. Single-cause fix will not hold — both changes must ship atomically.
projected_impact: |
  p99 N3 latency returns to ~9ms · slice-A SLA breach risk eliminated for 842 enterprise subscribers · $2.4M enterprise ARR cohort protected
triggering_incident_id: "slice-a-qos-drift"
validation_gates:
  - name: "Syntax"
    status: "pass"
    detail: "Candidate YAML schema-valid · SR Linux / CNF parser clean"
  - name: "Semantic"
    status: "pass"
    detail: "All referenced UPFs, slices, and PFCP session groups exist"
  - name: "Mission · utilization"
    status: "pass"
    detail: "Post-change link utilization stays under 90% on all paths"
  - name: "Mission · slice SLA"
    status: "pass"
    detail: "Slice-A p99 projected 8.9ms, well within 15ms SLA threshold"
  - name: "Digital twin"
    status: "pass"
    detail: "60s traffic replay against candidate: no SLA breach, no session drop"
  - name: "Policy"
    status: "pass"
    detail: "Change stays within approved QER enforcement policy envelope"
diff: |
  upf-01:
    qer_profiles:
      slice-a-priority:
        intent_gbr_mbps: 50
-       enforced_mbps: 32       # DRIFT -36% from intent
+       enforced_mbps: 50       # restored to intent
+       enforcement_mode: strict
    pfcp_sessions:
      slice-A:
-       high_bw_count: 637      # concentration on UPF-01
+       high_bw_count: 457      # 180 sessions migrated to UPF-02
  upf-02:
    pfcp_sessions:
      slice-A:
-       high_bw_count: 0
+       high_bw_count: 180      # balanced tail-distribution
